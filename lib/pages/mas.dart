// MasPage.dart
import 'package:flutter/material.dart';

class MasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Más'),
      ),
      body: Center(
        child: Text('Contenido de la página de Más'),
      ),
    );
  }
}
