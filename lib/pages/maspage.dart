import 'package:flutter/material.dart';

class MasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
       
      ),
      body: Center(
        child: Text('Esta es la página de mas'),
      ),
    );
  }
}
